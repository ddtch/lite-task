# Version control
.git
.gitignore

# Build output (rebuilt inside Docker)
_fresh/

# Local data (mounted as volumes in production)
task-light.db
data/

# Dev artifacts — the image runs `deno install` in the builder stage, so the
# host's node_modules never needs to travel with the build context.
node_modules/
.playwright-mcp/

# Not needed in the image
.github/
deploy/

# Docs / meta
TODO.md
*.md
!README.md
